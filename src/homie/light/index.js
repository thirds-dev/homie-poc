import sync from "./sync";
import api from "./api";
import off from "./off";
import on from "./on";
import state from "./state";

const light = {
  sync,
  api,
  on,
  off,
  state,
};

export default light;
